// src/theme.js
export const theme = {
  colors: {
    primary: '#2563eb',      // Primary blue from Tailwind config
    primaryLight: '#3b82f6', // Primary light from Tailwind config
    primaryDark: '#1d4ed8',  // Primary dark from Tailwind config
    secondary: '#50c878',    // Secondary green from Tailwind config
    secondaryLight: '#6ee7a0', // Secondary light from Tailwind config
    secondaryDark: '#45a663', // Secondary dark from Tailwind config
    background: '#f5f7fa',   // Background from Tailwind config
    backgroundCard: '#ffffff', // Card background from Tailwind config
    backgroundDark: '#333333', // Dark background from Tailwind config
    text: '#1f2937',         // Text from Tailwind config
    textLight: '#6b7280',    // Light text from Tailwind config
    textDark: '#111827',     // Dark text from Tailwind config
    border: '#e5e7eb',       // Border from Tailwind config
  },
  fonts: {
    sans: 'Inter, system-ui, sans-serif',
    serif: 'Georgia, serif',
  },
  shadows: {
    card: '0 4px 6px rgba(0, 0, 0, 0.1)',
    hover: '0 8px 16px rgba(0, 0, 0, 0.1)',
  },
};